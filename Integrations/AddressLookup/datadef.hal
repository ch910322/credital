begin
  
  BlockBegin(EBSAddrLookupBlock,-,"");
  LoadBlockOnStartup;
  CacheBlockInClient;
  RecordField(Math,M4Matrix,0,0);
  ArrayBegin(Math,-,1);
  ArrayField(CountryCode,M4Code,20,CountryVc);
  ArrayField(LookupType,M4Set,31501,0);
  EndArray;
  EndBlock;

  return;
end;