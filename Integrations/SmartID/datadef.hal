begin
  
  BlockBegin(SmartIDBlock,-,"");//now it's used for all signing integrations
  RecordField(AccessToken,M4Str,50,0);
  RecordField(Mode,M4Int,0,0);
  RecordField(SmartIDAccessToken,M4Str,50,0);
  RecordField(MarkIDMode,M4Int,0,0);
  RecordField(MarkIDAccessToken,M4Str,50,0);
  RecordField(DokobitDefault,M4Int,0,0);
  RecordField(MarkIDDefault,M4Int,0,0);
  EndBlock;

  return;
end;